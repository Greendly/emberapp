import { displayCategories } from '../../../helpers/display-categories';
import { module, test } from 'qunit';

module('Unit | Helper | display categories');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = displayCategories(42);
  assert.ok(result);
});
